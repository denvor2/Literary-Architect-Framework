# REVIEW — Add-UI-Style-Guide

## STATUS

FIX (только один пункт, узко — см. ниже; Sidebar.tsx полностью OK)

## Что уже правильно (не трогай)

Sidebar.tsx — кнопка "+ New Character" переделана верно
(border-zinc-300, rounded-md, компактный padding, hover-паттерн
консистентен с остальным Sidebar). Логика не тронута. Одобряю без
изменений.

## Что нужно исправить

docs/design/UI_STYLE_GUIDE.md — в разделе "Цветовая семантика"
эмодзи-кружки испорчены (похоже на двойную UTF-8 кодировку,
вероятно связано с Windows-терминалом): вместо 🔴🟢🟡🔵 в файле
реально записаны искажённые байты (\`ð\u009f\u0094´\` и подобное).

**Исправление — самое надёжное: убрать эмодзи полностью**, оставить
только текстовые названия цветов (они и так несут смысл через слово
"Красный"/"Зелёный" и т.п., эмодзи были чисто декоративными):

```markdown
## Цветовая семантика

- Красный (red-*) — ошибки, разрушительные действия
- Зелёный (green-*) — успех, подтверждение
- Жёлтый/янтарный (yellow-*/amber-*) — предупреждение
- Голубой (blue-*) — информационные сообщения, нейтральный статус
```

Остальное содержимое документа (разделы "Кнопки", "Общий стиль")
без изменений — проблема локализована только в этом одном списке.

После правки — проверь сам файл на диске (не только то, что было
скопировано в ARP) любым способом, который надёжно покажет реальные
байты (например, `cat` в терминале, а не через редактор, который
может тихо перекодировать при отображении) — чтобы убедиться, что
проблема не появится снова при следующей похожей задаче.

## NEXT STEP

Исправь эмодзи → уберу FIX, дай короткий ARP только с этим диффом
(не нужно пересылать весь файл/Sidebar.tsx заново — только
исправленный раздел "Цветовая семантика").
