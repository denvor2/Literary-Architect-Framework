# REVIEW — Rename-AIBus-Process-To-TaskBus (Инвентаризация, Шаг 1)

## STATUS

OK (для Шага 1). Решения ниже — авторизация на Шаг 2 с уточнённой,
СУЖЕННОЙ границей (не расширяю scope сегодня перед перерывом).

## Решения

### 1. docs/ai-bus/queue/done/ (33 файла)

**Согласен с твоей рекомендацией.** Переезжает только путь
(неизбежно, часть переименования папки). Текст внутри уже
заархивированных файлов НЕ переписывать — тот же принцип
append-only/historical record, что у EXECUTION_LOG.md. Это касается
и активных Step Card этой же задачи — правильно, что не трогал.

### 2. PROJECT_STATE.md:35 ("AI Bus v4 established...")

Подтверждаю твою классификацию — PROCESS. Переименовать в "Task Bus
v4" вместе с остальными PROCESS-строками в этом файле.

### 3. UNCERTAIN пункты 1-7 — сегодня НЕ РАСШИРЯЕМ SCOPE

Все перечисленные файлы (`docs/architecture/AI_BUS.md`,
`docs/project/07_AI_BUS.md`, `10_AI_BUS_WORKFLOW.md`,
`docs/reports/SPRINT-04.md`, `SPRINT_06_REPORT.md`,
`docs/project/15_MASTER_INDEX.md`, `docs/vision/...`) —
**оставить полностью нетронутыми сегодня**, независимо от того, что
внутри PROCESS или PRODUCT. Причина: они вне исходного Allowed paths
Step Card, и сейчас не время (конец дня, приближается пауза)
расширять scope переименования на находку, которую никто не
планировал. Это отдельная будущая уборка (старые дублирующие файлы
до консолидации в docs/ai-bus/), не сегодня.

Явно фиксирую как открытый пункт на будущее (не потерять):
**в репозитории существуют legacy-дубликаты процессной документации
вне docs/ai-bus/ — требуют отдельного решения (слить? удалить?
переименовать тоже?) в отдельной задаче.**

## Разрешение на Шаг 2 — точная граница на сегодня

Выполнить переименование ТОЛЬКО для файлов из твоих таблиц "1.
Полностью PROCESS" (все строки как есть) + PROJECT_STATE.md:35 по
пункту 2 выше. Ничего из UNCERTAIN не трогать. done/ — только путь,
не текст.

## NEXT STEP

Выполни Шаг 2 с этой границей. Приложи в ARP:
- git status --short (только docs/ai-bus/** → docs/task-bus/** и
  точечные правки в docs/project/*.md из таблицы 1)
- grep -rn "docs/ai-bus" . (кроме .git/) → ожидаемо НЕ ноль (legacy
  файлы из UNCERTAIN намеренно не обновлены) — опиши явно, где
  остались ссылки на старый путь и почему (по этому REVIEW).
- npm run build

Это последняя задача перед перерывом до завтра — не начинай ничего
нового после нее без явной команды.
