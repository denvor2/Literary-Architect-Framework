id: Sprint-09-Vision-Amendments
name: "Поправки к Sprint-09-Step-01 и Add-Vision-Doc (прочитать перед выполнением обоих)"
type: implementation

## Scope

Allowed paths:
- (не отдельная задача сама по себе — см. ниже, как применить)

## Objective

Это НЕ отдельный Step Card для выполнения по очереди — это поправки
к двум уже лежащим в pending/ задачам, которые ещё не начаты
(Sprint-09-Step-01.md и Add-Vision-Doc.md). Прочитай этот файл ПЕРЕД
началом каждой из них и примени поправки как часть их исполнения —
отдельного ARP на этот файл не нужно, отчитайся о поправках внутри
ARP соответствующей задачи.

## Поправка 1 — к Sprint-09-Step-01 (backend /api/reader)

Product Owner принял решение: первая версия продукта — интерфейс И
ответы ИИ на русском языке (английский — намного позже, ориентир
Sprint 30-40). Добавь в системный промпт Reader явную инструкцию
отвечать на русском языке (реакция читателя должна быть на русском,
независимо от языка входного текста, если не оговорено иное).

## Поправка 2 — к Add-Vision-Doc (docs/vision/BOOK_LEVEL_ASSISTANTS_VISION.md)

Добавь в конец файла (после существующего раздела "Что это НЕ
меняет") два новых раздела:

```markdown
## 6. Локализация — русский язык первой версии

Product Owner подтвердил (было решено ранее в предыдущих обсуждениях,
здесь зафиксирован конкретный график): первая версия продукта —
интерфейс и ответы ИИ полностью на русском языке. Английский —
отдельная локализация, ориентировочно на Sprint 30-40, не раньше.

**Статус:** решение принято, график зафиксирован. Практическое
следствие уже применено к Sprint-09-Step-01 (Reader) — системный
промпт инструктирует модель отвечать на русском. Существующие
промпты Line Editor (Sprint 04) и Critic (Sprint 08) написаны на
английском — ретроактивное приведение к русскому НЕ входит в текущий
scope, это отдельная будущая задача, если потребуется.

## 7. Reader как несколько именованных помощников; Critic — тематические подкатегории (будущее)

**Reader:** подтверждено соответствие уже существующему MVP Scope из
`PRODUCT_VISION.md`/`DOMAIN_MODEL.md` — там изначально заложено
"3-4 Readers". Предложение Product Owner: "Новый читатель" — это не
скрытая кнопка сброса истории внутри одного Reader, а отдельные
именованные карточки-помощники (несколько параллельных читателей,
можно сравнивать их реакции). Пока не реализовано.

**Critic:** будет один общий (уже реализован, Sprint 08) плюс
несколько тематических подкатегорий в будущем, по мотивам
изначального списка Expert'ов из ADR-0002 (не из более нового
illustrative-списка EXPERT_CATALOG.md Plot/Characters/Style/Dialogue/
Logic — выбран технический набор):

| Английское (ADR-0002) | Предлагаемое русское название | Примечание |
|---|---|---|
| Continuity Checker | Проверка согласованности сюжета | — |
| Fact Checker | Проверка фактов | — |
| Developmental Editor | Оценка сюжета | Слово "редактор" сознательно убрано — коллизия с уже существующей ролью Editor, у Critic-подкатегорий не должно быть слова "редактор" в названии, так как они дают оценку, а не правят текст |
| Style Editor | Оценка стиля | Та же причина |

**Статус:** оба пункта — зафиксированное намерение, НЕ реализовано.
Явно не входит в текущий Sprint 09 (Reader остаётся с формой ответа
"простой текст", один общий Reader; Critic остаётся один общий).
```

## Rules

- Только текстовые добавления, ничего не переписывай в уже
  существующем содержимом обеих задач.

## Validation

- В ARP по Sprint-09-Step-01: подтверди наличие русскоязычной
  инструкции в системном промпте (цитата строки).
- В ARP по Add-Vision-Doc: подтверди наличие разделов 6 и 7
  (grep -c "Локализация" и grep -c "тематические подкатегории").

## Stop Condition

Обычные условия обеих задач (не коммить до STATUS: OK) продолжают
действовать без изменений.
