id: Add-Deployment-Readiness-Vision-Note-Amendment
name: "Поправка к Add-Deployment-Readiness-Vision-Note: полный JSON-бэкап книги"
type: implementation

## Прочитать вместе с Add-Deployment-Readiness-Vision-Note.md перед выполнением

Если та задача ещё не выполнена — примени эту поправку как часть
раздела 11 при его создании. Если уже выполнена и закоммичена —
внеси эту поправку отдельным маленьким диффом в тот же файл, тем же
Allowed path.

## Поправка к разделу 11

После существующего текста раздела "## 11. Стандартное меню
приложения (к Sprint 30) + Импорт/Экспорт" добавь:

```markdown

**Уточнение от Product Owner:** форматы txt/docx/fb2/pdf — это
экспорт ДЛЯ ЧТЕНИЯ/ПУБЛИКАЦИИ (только текст книги, без персонажей и
метаданных структуры — теряется точность в одну сторону). Отдельно
нужен ПОЛНЫЙ формат — книга со всеми артефактами (главы, сцены,
персонажи, все поля) сохраняется в файл и полностью восстанавливается
в другом экземпляре приложения (перенос/резервная копия, без потерь).

Технически, после Sprint 11 Step 01 (когда Book станет самодостаточным
— все главы и персонажи внутри одного объекта), такой формат
реализуется тривиально как JSON-сериализация одного Book. Product
Owner решил НЕ вытаскивать это раньше отдельным шагом — оставить в
общем плане вместе с остальным Импортом/Экспортом к Sprint 30, чтобы
не дробить работу.
```

## Rules

- Только добавление текста, ничего существующего не менять/удалять.

## Validation

- grep -c "ПОЛНЫЙ формат" docs/vision/BOOK_LEVEL_ASSISTANTS_VISION.md → 1

## Output

ARP файлом в docs/task-bus/queue/active/ + в чат.

## Stop Condition

Не коммить до STATUS: OK от Architect.
