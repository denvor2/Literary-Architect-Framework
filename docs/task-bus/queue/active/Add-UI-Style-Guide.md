id: Add-UI-Style-Guide
name: "Добавить docs/design/UI_STYLE_GUIDE.md + переделать существующие кнопки"
type: implementation

## Scope

Allowed paths:
- docs/design/UI_STYLE_GUIDE.md (новый файл)
- apps/studio/src/components/Sidebar.tsx (только стиль кнопки
  "+ New Character" — не трогай остальную логику, это не относится
  к Step-04/Amendment, которые тоже правят этот файл; если Step-04
  или его Amendment уже выполнены/в работе на момент, когда возьмёшь
  эту задачу — согласуй с их состоянием, не перетирай их изменения)

Forbidden paths:
- всё остальное

## Objective

Product Owner зафиксировал конвенцию (стиль в духе Bootstrap):
кнопки должны визуально читаться как кнопки (рамка или заливка),
не как обычный текст/ссылка. Цветовая семантика: красный — ошибка/
необратимое действие, зелёный — успех, жёлтый — предупреждение,
голубой — информация.

### docs/design/UI_STYLE_GUIDE.md — создать дословно:

```markdown
# UI Style Guide

Зафиксировано с Product Owner 2026-07-05. Применяется ко всему
новому UI-коду; существующие элементы переводятся под эту конвенцию
по мере того, как до них доходит очередь (начиная с этой задачи).

## Кнопки

- Любой кликабельный элемент действия — настоящий `<button>`.
- Визуально каждая кнопка имеет рамку ИЛИ заливку — никогда просто
  текст без обводки/фона как единственный визуальный признак.
- Основное действие на экране (одно на вид) — заполненная кнопка
  (сплошной фон) — уже используется для "New Scene", это эталон.
- Второстепенные действия ("+ New X", переключатели и т.п.) —
  контурная кнопка (border, padding, скруглённые углы) — компактный
  размер допустим, но обязательно с рамкой.
- Разрушительные/необратимые действия (удаление) — красный акцент
  (border-red-*/text-red-* или аналог), не нейтральный серый.

## Цветовая семантика

- 🔴 Красный (red-*) — ошибки, разрушительные действия
- 🟢 Зелёный (green-*) — успех, подтверждение
- 🟡 Жёлтый/янтарный (yellow-*/amber-*) — предупреждение
- 🔵 Голубой (blue-*) — информационные сообщения, нейтральный статус

## Общий стиль

Простой, аккуратный, не перегруженный — в духе Bootstrap по
умолчанию: умеренное скругление углов, чёткие отступы, нейтральная
база (zinc, как сейчас), цвет — только там, где несёт смысл
(семантика выше), не как декоративный акцент.
```

### Sidebar.tsx — переделать "+ New Character"

Сейчас — обычный текст-ссылка (text-xs font-medium text-zinc-600,
без рамки). Переделать в контурную кнопку по конвенции выше: border,
padding, rounded — компактного размера, чтобы не выбивалась из ритма
строки заголовка секции. Не меняй логику (onClick/onCreateCharacter
остаются как есть) — только визуальные классы.

## Rules

- Только визуальные Tailwind-классы, никакой логики не менять.
- Документ — дословно как выше.

## Validation

- Живая проверка: "+ New Character" визуально читается как кнопка
  (видна рамка), не как ссылка.
- Приложи изменённый Sidebar.tsx (только диф по классам кнопки) и
  новый файл документа целиком.

## Output

ARP файлом в docs/task-bus/queue/active/ + в чат.

## Stop Condition

Не коммить до STATUS: OK от Architect.
