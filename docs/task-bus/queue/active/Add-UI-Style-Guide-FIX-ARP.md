# ARP — Add-UI-Style-Guide (FIX: эмодзи в UI_STYLE_GUIDE.md)

**Статус выполнения:** Готово к ревью

## Что сделано

`docs/design/UI_STYLE_GUIDE.md`, раздел "Цветовая семантика" — эмодзи убраны, остались только
текстовые названия цветов. `Sidebar.tsx` не трогал — по вашему указанию, уже одобрен.

## Диф

```diff
 ## Цветовая семантика

-- 🔴 Красный (red-*) — ошибки, разрушительные действия
-- 🟢 Зелёный (green-*) — успех, подтверждение
-- 🟡 Жёлтый/янтарный (yellow-*/amber-*) — предупреждение
-- 🔵 Голубой (blue-*) — информационные сообщения, нейтральный статус
++ Красный (red-*) — ошибки, разрушительные действия
++ Зелёный (green-*) — успех, подтверждение
++ Жёлтый/янтарный (yellow-*/amber-*) — предупреждение
++ Голубой (blue-*) — информационные сообщения, нейтральный статус
```

## Проверка сырых байтов (по вашей просьбе — не через редактор)

```
node -e "... [...text].filter(c => c.codePointAt(0) >= 0x1F000)" → NONE
cat docs/design/UI_STYLE_GUIDE.md (строки 20-25) → текст без искажений, эмодзи отсутствуют
```

**Честное наблюдение, не оспаривающее сам фикс:** когда я декодировал исходный файл (тот, что
уже лежал на диске) как UTF-8 напрямую через Node (`codePointAt`), эмодзи декодировались в
корректные, валидные кодпоинты (`U+1F534`, `U+1F7E2`, `U+1F7E1`, `U+1F535` — ровно
🔴🟢🟡🔵, без признаков двойного кодирования) — то есть сам файл на диске, при чтении мной,
не был повреждён. Возможно, искажение возникало не в файле, а в канале, которым его содержимое
попадало к вам на просмотр (терминал/копирование/промежуточный слой) — фиксирую это как
наблюдение для будущего, не как возражение: убрал эмодзи в любом случае, как вы и просили, раз
результат безопаснее и не теряет смысл (цвета и так названы словами).

## Отклонения

Нет. Изменение — ровно один раздел, `Sidebar.tsx` не тронут.

## Стоп-условие

Не коммичу — жду `REVIEW.md` со `STATUS: OK`.

Жду REVIEW.md.
