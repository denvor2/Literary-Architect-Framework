id: Sprint-14-Step-02-ARP
name: "ARP: UI — именованные экземпляры Reader, вкладки, сравнение бок о бок"
type: arp

## Что сделано

- **`AssistantPanel.tsx`** — добавлен новый под-компонент `ReaderPanel`, полностью заменяющий
  единый чат для режима Reader (Co-author/Editor/Critic рендерятся ровно как в Step 05, без
  изменений):
  - Ряд чипов-экземпляров, источник — `book.assistantThreads.reader` (весь массив, без нового
    пропа — уже доступен через существующий `book`).
  - Клик по имени чипа — выбор как активного (для отправки сообщений), инлайн-переименование
    (карандаш → `renameThread`), удаление (урна → `deleteThread`, задизейблена, если это
    последний экземпляр).
  - Переключатель "⇄" на каждом чипе — добавляет/убирает экземпляр из набора сравнения; при 2+
    выбранных — read-only сетка (responsive `lg:`) с последними сообщениями каждого экземпляра
    вместо единого чата (явное упрощение первой версии: ввод возможен только в single-view, не
    прямо в карточках сравнения — зафиксировано в самой Step Card).
  - "+ Новый читатель" — инлайн-форма имя+персонаж → `createThread("reader", {name, persona})`.
  - Отправка адресуется `thread.id` выбранного чипа, включает `persona` этого треда в payload
    `reader_reaction`.
- **`page.tsx`** — проброшены `renameThread`/`deleteThread` из контроллера (сам контроллер не
  тронут, готов с Step 01).

## Соответствие Scope

Изменены ровно 2 файла из Allowed paths (`git status --short` подтверждает). `domain/`,
`storage/`, `ai/`, `app/api/`, `useWorkspaceController.ts` не тронуты.

## Validation

- **`npx tsc --noEmit`** — 0 ошибок.
- **`npx eslint`** — 0 ошибок, 0 предупреждений.
- **`npx prettier --check`** — чисто (после `--write`).
- **`npm run build`** — успешно.
- **Живая проверка, часть 1 (pure-logic, без сети)** —
  `scratchpad/verify-sprint14-step02-logic.mjs`, тела скопированы дословно: 10/10 сценариев
  PASS — `toggleCompare` (иммутабельное добавление/удаление из набора сравнения),
  `buildOutgoing` (пустой/непустой ввод), `buildPayload` (persona попадает в payload только у
  треда, где она задана), выбор активного треда по умолчанию (последний элемент).
- **Живая проверка, часть 2 (реальный сервер + реальный Claude)** — `npx next start` на порту
  3419, `scratchpad/verify-sprint14-step02-live.mjs` воспроизводит ровно `handleSend` из
  `ReaderPanel` для двух разных именованных экземпляров ("Молодой читатель" —
  подросток-фэнтезист, "Опытный редактор" — профессионал 20 лет стажа) на один и тот же текст
  сцены: оба дали 200 OK, ответы реально разные по тону и содержанию (не заглушка), каждый
  ответ попал строго в свой тред, треды не перемешались (по 2 сообщения в каждом, не 4 в одном).
  5/5 PASS. Сервер остановлен после проверки.

## Отклонения от Step Card

Одно, явно предусмотренное самой Step Card как допустимое упрощение: в режиме сравнения бок о
бок ввод сообщений недоступен прямо в карточках сравнения — только просмотр истории; отправка
возможна только после переключения в single-view. Зафиксировано в Step Card заранее, не
самовольное отклонение.

## Stop Condition

Не закоммичено. Жду подтверждения Product Owner.
